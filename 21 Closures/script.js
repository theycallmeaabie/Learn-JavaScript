console.log("=== RETURNING FUNCTIONS & CLOSURES ===\nin javascript, functions can return other functions because functions are treated like values.\nwhen a parent function returns an inner function, that inner function can still access variables from the parent's lexical scope.\nthis behavior creates a closure.\nclosures allow returned functions to remember outer variables even after the parent function has finished execution.\nthis pattern is commonly used for private variables, factory functions, currying, and functional programming.\n");

// Closure = Function + Remembered Scope
console.log("=== CLOSURES IN DEPTH ===\na closure is formed when an inner function remembers variables from its outer lexical scope even after the outer function has finished execution.\nclosures happen because javascript preserves references to outer variables that are still being used by inner functions.\nclosures are based on lexical scoping.\nreturned inner functions can continue accessing outer variables because their lexical environment remains preserved in memory.\nclosures do not copy variables; they keep references to the original outer scope variables.\nclosures are commonly used for private variables, data hiding, currying, callbacks, and asynchronous programming.\n");

console.log("=== CLOSURES & MEMORY MANAGEMENT ===\nnormally, when a function finishes execution, its local variables become eligible for garbage collection and are removed from memory.\nhowever, if an inner function still references outer variables, javascript preserves those variables in a closure.\nclosures keep referenced lexical scope variables alive even after the parent function has completed execution.\nonly variables still being referenced are preserved; unused variables can still be garbage collected.\nclosures enable persistent state and private data but can also lead to memory leaks if large unused data remains referenced.\n");

console.log("=== NESTED CLOSURES & LEXICAL ENVIRONMENTS ===\nclosures can span multiple nested scopes.\nan inner function can access variables from its own scope, parent scope, grandparent scope, and global scope through the lexical scope chain.\nwhen nested functions reference variables from multiple outer levels, javascript preserves those lexical environments as linked closures.\na returned function is not just the function code itself; it is the function bundled together with references to its surrounding lexical environment.\nclosures work with named functions, anonymous functions, and arrow functions alike.\n");